spring:
  datasource:
    url: jdbc:postgresql://localhost/mnu
    username: mnu
    password: mnu
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false
      properties:
        hibernate:
          dialect: mnu.config.DbDialect